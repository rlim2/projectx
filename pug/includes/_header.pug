header
  .wrapper 
    img.headerLogo(src="img/headerUB.png" alt="Nada")
h1.page-title Optical & Ultrasonic Imaging Laboratory
nav
  .wrapper: ul.nav.nav-tabs
    li: a(href="#" class={active:currentPage === 'home'}) Home
    li: a(href="#" class={active:currentPage === 'research'}) Research 
    li: a(href="#" class={active:currentPage === 'publications'}) Publications
    li: a(href="#" class={active:currentPage === 'members'}) Members
    li: a(href="#" class={active:currentPage === 'position'}) Position
    li: a(href="#" class={active:currentPage === 'teaching'}) Teaching
    li: a(href="#" class={active:currentPage === 'media'}) Media
